Django==6.1
djangorestframework==3.18.0
django-cors-headers==4.9.0
psycopg[binary]==3.3.4
requests==2.34.2
httpx==0.28.1
docker==7.2.0
beautifulsoup4==4.15.0
channels==4.3.2
channels_redis==4.3.0
# channels_redis's blocking BRPOP relies on redis-py behavior where a
# blocking command's timeout can exceed the client's socket_timeout and
# return nil rather than raising. redis-py 5+ enforces the opposite
# (blocking `timeout` cannot exceed `socket_timeout` — see
# redis/redis-py#2807), so leaving `redis` unpinned lets pip pull 8.x and
# every idle BRPOP cycle raises TimeoutError, killing the ASGI consumer and
# force-closing every open chat WebSocket every few seconds.
redis==4.6.0
daphne==4.2.3
langchain-core==1.5.3
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
pydantic==2.13.4
ollama==0.6.2
pgvector==0.5.0
pypdf==6.15.0
python-docx==1.2.0
mcp==1.29.0
